<%= form_for(@operador) do |f| %>
  <% if @operador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@operador.errors.count, "error") %> prohibited this operador from being saved:</h2>

      <ul>
      <% @operador.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :matricula %><br>
    <%= f.number_field :matricula %>
  </div>
  <div class="field">
    <%= f.label :dateniver %><br>
    <%= f.date_select :dateniver %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
